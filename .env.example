# Default/primary setup: EPC blob JSON
# If unset, app uses built-in default blob URL from api/heetsa.js.
# Set this only when you want to use your own Vercel Blob JSON URL.
EPC_BLOB_URL=

# Optional: better address lookup via OS Places Find API
OS_PLACES_API_KEY=

# Optional: live EPC Open Data lookup (not required for blob-only deployment)
EPC_REGISTER_API_KEY=
EPC_REGISTER_EMAIL=

# Optional: live DNO/SSEN headroom overlay endpoint (+ optional auth key)
DNO_API_URL=
DNO_API_KEY=

# Optional: override SPEN heatmap endpoint
SPEN_HEATMAP_API_URL=

# Vercel setup:
# 1) Copy these values into Vercel > Project > Settings > Environment Variables
# 2) Save for Production (and Preview if needed)
# 3) Redeploy the project
